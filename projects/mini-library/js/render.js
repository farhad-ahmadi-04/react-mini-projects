"use strict";

/**
 * getting render you pass 2 position and get render
 * @param {Element} ele1 - position for append
 * @param {*} ele2 - position for append to
 */
export default function render(ele1, ele2) {
    ele1.appendChild(ele2);
}